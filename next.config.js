/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/PiyushWebsite",
  assetPrefix: "/PiyushWebsite",
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
